{
  "spec": {
    "rules": {
      "grafana1": {
        "path": "${blueprint.self.secrets.non-existing-secret}",
        "port": "${service.be.out.interfaces.http.port}"
      }
    }
  },
  "disabled": true
}